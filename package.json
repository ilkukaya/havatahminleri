{
  "name": "yarinhava",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "node scripts/fetch-weather.mjs && astro build && node scripts/validate-build.mjs",
    "build:offline": "node scripts/make-fixture-cache.mjs && astro build && node scripts/validate-build.mjs --allow-synthetic",
    "validate": "node scripts/validate-build.mjs",
    "smoke": "node scripts/smoke-test.mjs",
    "test": "node --experimental-strip-types --test \"tests/*.test.mjs\"",
    "preview": "astro preview"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.2",
    "@tailwindcss/vite": "^4.2.2",
    "astro": "^6.1.1",
    "tailwindcss": "^4.2.2"
  }
}
