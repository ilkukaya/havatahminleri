---
import provinces from '../data/provinces.json';
import regions from '../data/regions.json';

const regionGroups = regions.map((r: any) => ({
  ...r,
  provinces: provinces.filter((p: any) => p.region === r.id),
}));
---

<footer class="bg-[var(--color-surface)] border-t border-[var(--color-border)] mt-12">
  <!-- All cities by region -->
  <div class="max-w-7xl mx-auto px-4 py-10">
    <h2 class="text-lg font-bold mb-6 text-[var(--color-text)]">Türkiye Hava Durumu</h2>
    <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-6">
      {regionGroups.map((region: any) => (
        <div>
          <h3 class="text-sm font-semibold text-primary-600 dark:text-primary-400 mb-2">{region.name}</h3>
          <div class="flex flex-wrap gap-x-3 gap-y-1">
            {region.provinces.map((p: any) => (
              <a
                href={`/${p.slug}-hava-durumu/`}
                class="text-xs text-[var(--color-text-secondary)] hover:text-primary-500 transition-colors"
              >
                {p.name}
              </a>
            ))}
          </div>
        </div>
      ))}
    </div>
  </div>

  <!-- Bottom bar -->
  <div class="border-t border-[var(--color-border)]">
    <div class="max-w-7xl mx-auto px-4 py-4 flex flex-col sm:flex-row items-center justify-between gap-3">
      <div class="flex items-center gap-2 text-xs text-[var(--color-text-secondary)]">
        <svg class="w-5 h-5 shrink-0" viewBox="0 0 48 48" fill="none">
          <defs>
            <linearGradient id="ftr-bg" x1="0" y1="0" x2="1" y2="1">
              <stop offset="0%" stop-color="#2563EB"/>
              <stop offset="100%" stop-color="#7C3AED"/>
            </linearGradient>
            <linearGradient id="ftr-sun" x1="0" y1="0" x2="0" y2="1">
              <stop offset="0%" stop-color="#FBBF24"/>
              <stop offset="100%" stop-color="#F59E0B"/>
            </linearGradient>
          </defs>
          <rect width="48" height="48" rx="14" fill="url(#ftr-bg)"/>
          <circle cx="33" cy="14" r="7" fill="url(#ftr-sun)"/>
          <ellipse cx="18" cy="24" rx="10" ry="6.5" fill="white" opacity="0.95"/>
          <ellipse cx="27" cy="22" rx="8" ry="6" fill="white" opacity="0.95"/>
          <ellipse cx="12" cy="25" rx="6" ry="4.5" fill="white" opacity="0.9"/>
          <path d="M16 33 L21 38 L21 43" stroke="white" stroke-width="2.8" stroke-linecap="round" stroke-linejoin="round" fill="none"/>
          <path d="M26 33 L21 38" stroke="white" stroke-width="2.8" stroke-linecap="round" stroke-linejoin="round" fill="none"/>
        </svg>
        <span>© {new Date().getFullYear()} yarınhava.com — Tüm hakları saklıdır.</span>
      </div>
      <nav class="flex items-center gap-4">
        <a href="/hakkimizda/" class="text-xs text-[var(--color-text-secondary)] hover:text-primary-500 transition-colors">
          Hakkımızda
        </a>
        <a href="/gizlilik-politikasi/" class="text-xs text-[var(--color-text-secondary)] hover:text-primary-500 transition-colors">
          Gizlilik Politikası
        </a>
        <a href="/kullanim-sartlari/" class="text-xs text-[var(--color-text-secondary)] hover:text-primary-500 transition-colors">
          Kullanım Şartları
        </a>
        <a href="/iletisim/" class="text-xs text-[var(--color-text-secondary)] hover:text-primary-500 transition-colors">
          İletişim
        </a>
      </nav>
    </div>
  </div>
</footer>
